{% extends 'base.html.twig' %}

{% block title %}Anglais{% endblock %}

{% block body %}
    <div class="container">
        {% if lyceeAnglais is not empty %}
            {% for key, lyceeAnglais in lyceeAnglais %}
                <div class="col-11">
                    <div class="row">
                        <div class="col-12 text-center">
                            <div>
                                <h1 class="custom-title">{{ lyceeAnglais.editableTitle | default('Titre non défini') }}</h1>
                            </div>

                            <div class="container_dys_description">
                                <h5>{{ lyceeAnglais.description | default('') | linkify | nl2br }}</h5>
                                <h5>{{ lyceeAnglais.description1 | default('') | linkify | nl2br }}</h5>
                                <h5>{{ lyceeAnglais.description2 | default('') | linkify | nl2br }}</h5>
                                <h5>{{ lyceeAnglais.description3 | default('') | linkify | nl2br }}</h5>
                                <h5>{{ lyceeAnglais.description4 | default('') | linkify | nl2br }}</h5>
                                <h5>{{ lyceeAnglais.description5 | default('') | linkify | nl2br }}</h5>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% else %}
            <p>Aucune entrée trouvée les langues.</p>
        {% endif %}

        <div class="buton_modifier_dys">
            <div class="col-12">
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_BUREAU') %}
                    <div class="container_dys_description_button text-center">
                        <a href="{{ path('app_lyceeAnglais_edit') }}" class="boutton_dys btn btn-primary btn-lg">Modifier</a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}